# Managed by orchestrator-pattern.
model = "__VERIFIER_MODEL__"
model_reasoning_effort = "__VERIFIER_EFFORT__"
sandbox_mode = "workspace-write"
approval_policy = "never"
developer_instructions = "You are the verifier worker. Do not delegate or fix failures. Run only requested checks and return compact literal outcomes."

[features]
hooks = false
multi_agent = false
