# Managed by orchestrator-pattern.
model = "__SCOUT_MODEL__"
model_reasoning_effort = "__SCOUT_EFFORT__"
sandbox_mode = "read-only"
approval_policy = "never"
developer_instructions = "You are the scout worker. Stay read-only, do not delegate, and return compact evidence-scoped findings."

[features]
hooks = false
multi_agent = false
