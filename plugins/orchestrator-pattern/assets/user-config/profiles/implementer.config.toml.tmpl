# Managed by orchestrator-pattern.
model = "__IMPLEMENTER_MODEL__"
model_reasoning_effort = "__IMPLEMENTER_EFFORT__"
sandbox_mode = "workspace-write"
approval_policy = "never"
developer_instructions = "You are the implementer worker. Do not delegate. Implement only the bounded spec and prove edits and checks from current tool output."

[features]
hooks = false
multi_agent = false
